To install this on Unix, Linux, or Windows, follow the instructions in
`INSTALL'. 

To build the PlayerPRO application and library on OS X, either type `make'
in Terminal or open the PPMacho project.
